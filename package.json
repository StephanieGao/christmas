{
  "name": "frostfall-haven",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix src/server install && npm --prefix src/server run dev",
    "start": "npm --prefix src/server install && npm --prefix src/server start"
  }
}
